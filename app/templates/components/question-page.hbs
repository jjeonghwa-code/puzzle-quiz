<h2>Question {{questionNumber}}/{{questionsCount}}</h2>

{{#liquid-unless isAnswerCorrect}}
  <h3>{{question.text}}</h3>
{{/liquid-unless}}

{{answer-display
  class="answer-display"
  currentAnswer=currentAnswer
  correctAnswer=question.answer
  isAnswerCorrect=isAnswerCorrect
}}

{{#liquid-unless isAnswerCorrect}}
  {{answer-input
    correctAnswer=question.answer
    onAnswerChanged=(action (mut currentAnswer))
  }}
{{else}}
  {{#if nextQuestion}}
    <button {{action (route-action "goToNextQuestion")}}>
      Next
    </button>
  {{else}}
    <button {{action (route-action "goToAnagram")}}>
      Finish
    </button>
  {{/if}}
{{/liquid-unless}}
